import { INestApplication } from '@nestjs/common';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';

/**
 * For swagger document
 * @param app
 */
export async function swagger(app: INestApplication) {
  // For user API documents

}
